/* Write your global styles here, in PostCSS syntax */
@tailwind base;
@tailwind components;
@tailwind utilities;

*{
  background-color: var(
    --theme-bg-color
  );

}

html,
body,
#app {
  height: 100%; /* Make sure html and body tags are also full height */
  margin: 0;
  padding: 0;
}
